---
permalink: "{{ page.fileSlug }}/index.html"
layout: layouts/base.njk
title: Подкаст &laquo;Ёжик в&nbsp;нирване&raquo;
description: Мой подкаст о&nbsp;различных аспектах йоги и&nbsp;всего связанного с&nbsp;ней. Обсуждение острых тем, история и&nbsp;философия, интервью с&nbsp;интересными, знающими людьми, обзоры книг, курсов и&nbsp;многое другое.
eleventyNavigation:
  key: Подкаст &laquo;Ёжик в&nbsp;нирване&raquo;
  order: 4
---

{% from "macros/episodes.njk" import episodes %}
{% from "macros/playlists.njk" import playlists %}
{% from "macros/stage.njk" import stage %}

<div id="main">
    <div class="inner inner_style_podcast">
        <header id="header" class="header">
            <div class="header__nav">
                <a href="{{ '/podcast/' | url }}" class="header__logo">
                    <strong>{{ metadata.podcast.name | safe }}</strong>
                </a>
            </div>
            <ul class="icons header__icons header__icons_color_dark">
                {% for link in metadata.podcast.socials %}
                    <li>
                        <a href="{{ link.url }}" class="icon brands fa-{{ link.icon }}">
                            <span class="label">{{ link.title }}</span>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </header>

        {{ stage({
            'title': title,
            'subtitle': description,
            'actions': [
                {
                    'url':  '/podcast/episodes/',
                    'title': 'Все выпуски подкаста'
                }
            ],
            'image': '/images/podcast/logo-square.jpg',
            'imageAlt': 'Человекоподобный ёж в позе лотоса держит яблоко'
        }) }}
    </div>
    <div class="inner">
        {{ episodes({
            'episodes': [collections.episodes | reverse | first],
            'title': 'Новый выпуск'
        }) }}

        {{ playlists({
            'playlists': collections.playlists,
            'title': 'Сборники выпусков'
        }) }}
    </div>
</div>
